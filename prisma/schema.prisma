generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Models go here — see project plan (roadside-assistance-project-plan.md)
// for the full schema: User, MechanicProfile, ServiceRequest, Payment, Review
